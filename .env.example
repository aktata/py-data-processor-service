# 服务基础配置
APP_NAME=py-data-processor-service
APP_ENV=local
APP_VERSION=0.1.0
LOG_LEVEL=INFO

# 上传与解析限制（默认已可用）
MAX_UPLOAD_SIZE_MB=5
JSON_MAX_DEPTH=20
JSON_MAX_NODES=20000

# 监听地址与端口
HOST=0.0.0.0
PORT=8000
